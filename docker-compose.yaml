services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    networks:
      - agent_network
    restart: unless-stopped

volumes:
  qdrant_storage:

networks:
  agent_network:
    driver: bridge
